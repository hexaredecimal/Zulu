-module(io). 
-opt(). 

write(X) ->
  SystemClass = ffi:load_class("","java.lang.System"),
	OutField = ffi:get_field(SystemClass, "out"), 
  PrintStreamClass = ffi:load_class("","java.io.PrintStream"),
	ffi:invoke(Method, OutField, X).

